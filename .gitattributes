# Normalize line endings — text files always stored with LF in the repository
* text=auto

# Shell scripts and configs that run inside Linux containers must use LF
*.sh       text eol=lf
*.yml      text eol=lf
*.yaml     text eol=lf
Dockerfile text eol=lf
